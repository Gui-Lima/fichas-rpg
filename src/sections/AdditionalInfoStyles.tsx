import {makeStyles} from "@material-ui/core"

const useStyles = makeStyles((theme) => ({
  container: {
    paddingTop: 8,
    paddingLeft: 12,
    paddingRight:12,
    paddingBottom: 8,
    borderRadius: 4,
    borderStyle: "solid",
    borderWidth: 1,
    borderColor: "#FFF"
  }
}))

export default useStyles