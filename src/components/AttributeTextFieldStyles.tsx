import {makeStyles} from "@material-ui/core"

const useStyles = makeStyles((theme) => ({

  iconButtonRoot: {
    padding: 0,
    color: "#FFF"
  },

  svgRoot: {
    fontSize: "1.7rem"
  }

}))

export default useStyles